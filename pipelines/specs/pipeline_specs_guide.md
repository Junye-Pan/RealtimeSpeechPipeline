# Pipeline Specs Scaffold

Canonical MVP spec artifact:

- `pipelines/specs/voice_assistant_spec_v1.json`

This file is the versioned `PipelineSpec` baseline referenced by the MVP bundle and validated in scaffold contract tests.
