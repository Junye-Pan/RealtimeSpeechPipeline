# Terraform Deployment Scaffold

MVP Terraform scaffold is implemented under `deploy/terraform/mvp_single_region/`:

- `main.tf`
- `variables.tf`

This scaffold anchors profile wiring for deterministic deployment verification and release handoff.
