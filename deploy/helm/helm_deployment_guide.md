# Helm Deployment Scaffold

MVP Helm scaffold is implemented under `deploy/helm/rspp-mvp/`:

- `Chart.yaml`
- `values.yaml`
- `templates/runtime-deployment.yaml`

The chart provides the canonical MVP runtime deployment packaging surface and is covered by scaffold contract checks.
